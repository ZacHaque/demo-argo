apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-color.fullname" . }}
  labels:
    {{- include "app-color.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-color.selectorLabels" . | nindent 4 }}
